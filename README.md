Several scripts to support the Gold Standard generaiton process. Intent is that everything here is
standalone enough to run as command line, cron scripts or to be used as a library from a larger app.
